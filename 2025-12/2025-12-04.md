# Spring カスタムバリデーション

## @interface
 - アノテーションを定義するときは普通の`interface`ではなく、`@interface`を使用する。
 - `@interface`を使用したクラスはコンパイル後`public interface MyAnno extends java.lang.annotation.Annotation`
## @Documented

## @Constraint

## @Target

## @Retention
