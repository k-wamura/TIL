# Spring カスタムバリデーション

## @interface
 - アノテーションを定義するときは普通の`interface`ではなく、`@interface`を使用する。
 - `@interface`を使用したクラスはコンパイル後`java.lang.annotation.Annotation`を継承したクラスになる。

## @Documented
 - 

## @Constraint

## @Target

## @Retention
